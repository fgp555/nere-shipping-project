<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reporte de Productos</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 20px;
      }
      h1 {
        text-align: center;
        font-size: 24px;
      }
      pre {
        background-color: #f4f4f4;
        padding: 15px;
        border: 1px solid #ddd;
        border-radius: 5px;
        font-size: 14px;
        overflow-x: auto;
        white-space: pre-wrap; /* Permite que el contenido se ajuste en el PDF */
      }
    </style>
  </head>
  <body>
    <h1>Listado de Productos</h1>
    <pre><%= JSON.stringify(data, null, 2) %></pre>
  </body>
</html>
