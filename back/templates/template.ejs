<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Documento PDF</title>
    <style>
        body { font-family: Arial, sans-serif; }
        h1 { color: #333; }
        img { width: 200px; height: auto; }
    </style>
</head>
<body>
    <h1>Hola, <%= nombre %>!</h1>
    <p>Este es un documento PDF generado dinámicamente con los siguientes productos:</p>
    <ul>
        <% productos.forEach(producto => { %>
            <li>
                <h2><%= producto.nombre %></h2>
                <img src="<%= producto.imagenPath %>" alt="<%= producto.nombre %>">
            </li>
        <% }) %>
    </ul>
</body>
</html>
